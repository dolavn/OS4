#include "types.h"
#include "user.h"



int main(int argc, char** argv){

  if(argc < 2 || argc > 4) {
    printf(2, "Usage: find <path> [<options>] [<tests>]\n");
    exit();
  }

  
  // printf(2, "aaa\n");
  exit();
}
